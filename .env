# Database configuration (using SQLite locally, Vercel Postgres when deployed)
DATABASE_URL="file:./dev.db"

# Admin Authentication
JWT_SECRET="dionne-admin-jwt-v1-98f7e6d5c4b3a210qweasdzxc"

# Vercel Blob Storage (Add this when deploying for image uploads)
BLOB_READ_WRITE_TOKEN="add_your_vercel_blob_token_here"

# Zoho Email Configuration
# The system connects to Zoho's secure SMTP server to send newsletter broadcasts.
SMTP_HOST="smtp.zoho.com"
SMTP_PORT="465"
SMTP_USER="hello@dionnetweneboah.com"
SMTP_PASS="your_zoho_app_password_here"
